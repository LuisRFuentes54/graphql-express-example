type Person {
    id: Int!
    name: String!
    lastname: String
}

type Query {
    "Devuelve todas las personas"
    getPersons: [Person]
    "Devuelve una persona"
    getPerson(id: ID!): Person
}